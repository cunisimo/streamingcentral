# Top Yump — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sección `/top` con acceso propio en el nav inferior, que muestra el top 10 de seis plataformas en Argentina, con Netflix en dato oficial de consumo y las otras cinco en popularidad de TMDB, etiquetada como tal.

**Architecture:** Un cron semanal de Vercel ingesta el TSV público de Netflix a una tabla de Supabase que además hace de mapa título→TMDB. Un endpoint `/api/top` arma los bloques (los del usuario primero, el resto después) leyendo esa tabla para Netflix y `discover` por popularidad para las demás. La UI es un `TopView` cliente con un toggle Películas/Series global.

**Tech Stack:** Next.js 14 App Router, TypeScript, Supabase (Postgres + RLS), Upstash Redis, TMDB v4, Vercel Cron.

**Spec:** `docs/superpowers/specs/2026-08-09-top-yump-design.md`

## Global Constraints

- Todo el texto de la UI en español rioplatense.
- Sin CSS-in-JS: todo estilo nuevo va en `app/globals.css`, reusando clases existentes antes de inventar una nueva.
- Las rutas API son finas: parsean query params y llaman a una función de `lib/`.
- Región fija `AR`, `with_watch_monetization_types=flatrate`.
- Toda llamada a TMDB pasa por `lib/tmdb.ts`, que ya tiene el semáforo de concurrencia (`MAX_EN_VUELO`, 24). **No agregar un limitador propio.**
- En el Top **no** corren los filtros de `lib/audience.ts` (no se pasa `scope`) ni el dedup del Home.
- Netflix es `provider_id` 8 en TMDB.
- El toggle es **uno solo global** para toda la página, no uno por bloque.
- Copy exacto de los subtítulos: Netflix → `Lo más visto esta semana · dato oficial`; las demás → `Lo más popular ahora`.
- Etiqueta del nav: `Top` (no "Top Yump"). Título de la página: `Top Yump`.

### Nota sobre verificación (leer antes de empezar)

**Este proyecto no tiene runner de tests.** No hay jest, vitest ni `node --test` configurado; `package.json` solo expone `dev`, `build`, `start`, `lint`. El ciclo de verificación real del proyecto es `npx tsc --noEmit` + `npx next build` + comprobaciones manuales contra `next dev`.

Por eso este plan **no sigue TDD estricto**: cada tarea termina en una verificación ejecutable con comandos exactos y salida esperada, pero no en un test automatizado. Es una desviación consciente del default de la skill, no un olvido.

Si se prefiere TDD real, hay que sumar vitest antes de la Tarea 3 (una dependencia, config mínima, y las funciones puras de `lib/netflix-top10.ts` — `normalizeTitle` y `parseArWeek` — son las que lo justifican). No está incluido en este plan porque el dueño no lo pidió.

---

## Estructura de archivos

| Archivo | Responsabilidad |
|---|---|
| `supabase/schema.sql` (modificar) | tabla `netflix_top10` + RLS |
| `lib/providers-ar.ts` (modificar) | sacar id 2 de Apple TV+ y la entrada Star+ |
| `lib/tmdb.ts` (modificar) | nuevo export `searchTitles(type, query)` |
| `lib/netflix-top10.ts` (crear) | parseo del TSV, normalización, resolución a TMDB, ingesta y lectura de la última semana |
| `app/api/cron/netflix-top10/route.ts` (crear) | handler del cron, autenticado con `CRON_SECRET` |
| `vercel.json` (crear) | declaración del cron |
| `lib/top.ts` (crear) | arma `{ mine, others }` combinando Netflix (tabla) y popularidad (discover) |
| `app/api/top/route.ts` (crear) | ruta fina sobre `lib/top.ts` |
| `components/TopView.tsx` (crear) | UI de la sección: toggle global, separadores, bloques |
| `app/top/page.tsx` (crear) | página |
| `components/TitleCard.tsx` (modificar) | prop opcional `rank?: number` |
| `components/BottomNav.tsx` (modificar) | quinto ítem "Top" |
| `app/globals.css` (modificar) | `.rank-num`, `.top-sub`, `.top-info`, `.rank-slot` |

---

## Task 1: Corregir el mapeo de plataformas (prerrequisito)

Saca el `provider_id` 2 de Apple TV+ — es la tienda de alquiler y compra, y TMDB la devuelve bajo `flatrate` en AR, inflando el catálogo de 321 títulos reales a 5389. Saca también Star+, que se fusionó con Disney+ en 2024 y devuelve 0.

Es un bug existente en toda la app, no solo en el Top: hoy quien elige Apple TV+ ve 5068 películas de alquiler como si estuvieran incluidas.

**Files:**
- Modify: `lib/providers-ar.ts:24` (Apple TV+) y `lib/providers-ar.ts:36` (Star+)

**Interfaces:**
- Consumes: nada.
- Produces: `PLATFORMS` con 14 entradas y Apple TV+ en `tmdbIds: [350, 2243]`. Ninguna firma cambia.

- [ ] **Step 1: Medir el estado actual para tener contra qué comparar**

```bash
node -e "const t=require('fs').readFileSync('.env.local','utf8').match(/^TMDB_READ_TOKEN=(.+)\$/m)[1].trim();(async()=>{for(const[n,ids]of[['Apple TV+ actual','350|2|2243'],['Apple TV+ propuesto','350|2243']]){const u=new URL('https://api.themoviedb.org/3/discover/movie');u.searchParams.set('watch_region','AR');u.searchParams.set('with_watch_monetization_types','flatrate');u.searchParams.set('with_watch_providers',ids);const r=await fetch(u,{headers:{Authorization:'Bearer '+t}});console.log(n,(await r.json()).total_results)}})()"
```

Expected: `Apple TV+ actual 5146` y `Apple TV+ propuesto 104` (los números pueden moverse un poco; lo que importa es el orden de magnitud).

- [ ] **Step 2: Aplicar el cambio**

En `lib/providers-ar.ts`, reemplazar la línea de Apple TV+:

```ts
  { code: "at", name: "Apple TV+",     tmdbIds: [350, 2243],      color: "#111111" },
```

y borrar por completo la línea de Star+:

```ts
  { code: "sp", name: "Star+",         tmdbIds: [619],            color: "#5A2EAE" },
```

Agregar debajo del comentario de Pluto TV (`lib/providers-ar.ts:46-49`) esta nota:

```ts
// El id 2 ("Apple TV") NO va en Apple TV+: es la tienda de alquiler/compra, y
// TMDB la devuelve bajo `flatrate` en AR (misma inconsistencia que Pluto TV).
// Con el id 2 puesto, Apple TV+ pasaba de 321 títulos reales a 5389, y el
// usuario veía cine de alquiler como si estuviera incluido en la suscripción.
// Star+ (619) se sacó porque se fusionó con Disney+ en 2024: devuelve 0 en
// movie y en tv.
```

- [ ] **Step 3: Verificar que compila y que el tipo `PlatformCode` no quedó huérfano**

Run: `npx tsc --noEmit`
Expected: sin salida (0 errores).

Si `PlatformCode` en `lib/types.ts` enumera `"sp"` explícitamente, dejarlo: sacarlo rompería el `localStorage` de un usuario que ya la tenía elegida. `platformByCode("sp")` va a devolver `undefined`, que es el comportamiento correcto para una plataforma que ya no existe.

- [ ] **Step 4: Commit**

```bash
git add lib/providers-ar.ts
git commit -m "fix(providers): sacar la tienda Apple TV (id 2) y Star+"
```

---

## Task 2: Tabla `netflix_top10`

**Files:**
- Modify: `supabase/schema.sql` (agregar al final)

**Interfaces:**
- Consumes: nada.
- Produces: tabla `netflix_top10` con PK `(week, category, rank)` y columnas `raw_title text not null`, `tmdb_id int`, `needs_review boolean not null default false`.

- [ ] **Step 1: Agregar la tabla al final de `supabase/schema.sql`**

```sql
-- ---------------------------------------------------------------------------
-- Top 10 semanal de Netflix en Argentina.
-- Fuente: https://www.netflix.com/tudum/top10/data/all-weeks-countries.tsv
-- (público, sin auth, se actualiza los martes con la semana cerrada el domingo)
--
-- La tabla cumple DOS funciones: es el ranking y es el mapa título→TMDB. El TSV
-- solo trae el título en inglés, sin id ni año, así que resolverlo cuesta 1-2
-- búsquedas en TMDB por título nuevo. Como los títulos duran varias semanas en
-- el top, consultando acá primero se resuelven 2-4 por semana en vez de 20.
-- Efecto buscado: una corrección manual de `tmdb_id` queda fija para siempre.
create table if not exists netflix_top10 (
  week         date    not null,
  category     text    not null check (category in ('movie','tv')),
  rank         int     not null check (rank between 1 and 10),
  raw_title    text    not null,   -- el título tal cual viene del TSV
  tmdb_id      int,                -- null si no se pudo resolver
  needs_review boolean not null default false,
  updated_at   timestamptz not null default now(),
  primary key (week, category, rank)
);

create index if not exists netflix_top10_raw_title_idx on netflix_top10 (raw_title, category);

-- RLS: lectura pública (es catálogo, no dato personal). Sin políticas de
-- escritura: solo el service_role del cron escribe, y bypassa RLS.
alter table netflix_top10 enable row level security;
drop policy if exists "lectura pública" on netflix_top10;
create policy "lectura pública" on netflix_top10 for select using (true);
```

- [ ] **Step 2: Aplicar el SQL en Supabase**

Correr ese bloque en el SQL editor del proyecto de Supabase. Es idempotente (`if not exists`, `drop policy if exists`), así que se puede re-correr el `schema.sql` entero sin romper nada.

- [ ] **Step 3: Verificar que la tabla existe y que anon puede leerla**

En el SQL editor:

```sql
select count(*) from netflix_top10;
```

Expected: `0`.

- [ ] **Step 4: Commit**

```bash
git add supabase/schema.sql
git commit -m "feat(db): tabla netflix_top10 (ranking + mapa de títulos)"
```

---

## Task 3: `lib/netflix-top10.ts` — parseo, resolución e ingesta

**Files:**
- Modify: `lib/tmdb.ts` (agregar `searchTitles`)
- Create: `lib/netflix-top10.ts`

**Interfaces:**
- Consumes: `watchProviders(type, id)` de `lib/tmdb.ts`; `supabaseServer()` de `lib/supabase.ts`.
- Produces:
  - `searchTitles(type: MediaType, query: string): Promise<Paged<RawTitle>>` en `lib/tmdb.ts`
  - `normalizeTitle(s: string): string`
  - `parseArWeek(lines: string[]): NetflixRow[]` donde `NetflixRow = { week: string; category: "movie" | "tv"; rank: number; rawTitle: string }`
  - `ingestLatestWeek(): Promise<{ week: string; inserted: number; resolved: number; review: number }>`
  - `latestWeekRows(): Promise<{ week: string; movie: StoredRow[]; tv: StoredRow[] }| null>` donde `StoredRow = { rank: number; rawTitle: string; tmdbId: number | null }`

- [ ] **Step 1: Agregar `searchTitles` a `lib/tmdb.ts`**

Justo debajo de `searchMulti` (línea 129-133):

```ts
// Búsqueda acotada a un tipo. La usa la ingesta del top 10 de Netflix, que
// recibe títulos EN INGLÉS: por eso fuerza `language`, pisando el es-ES de
// DEFAULTS. Sin eso, "The Running Man" no matchea contra el título en español.
export function searchTitles(type: MediaType, query: string) {
  return tmdb<Paged<RawTitle>>(`/search/${type}`, {
    query, include_adult: "false", language: "en-US", page: "1",
  });
}
```

- [ ] **Step 2: Crear `lib/netflix-top10.ts`**

```ts
import "server-only";
import { searchTitles, watchProviders } from "./tmdb";
import { supabaseServer } from "./supabase";
import type { MediaType } from "./types";

// Top 10 semanal de Netflix en Argentina.
//
// El TSV es público y pesa 31 MB, pero está ordenado por país ascendente y
// semana descendente: Argentina de la última semana son las PRIMERAS 20 filas
// de datos. Por eso se lee el stream y se corta; no se baja el archivo entero.
const TSV_URL = "https://www.netflix.com/tudum/top10/data/all-weeks-countries.tsv";
const NETFLIX_PROVIDER_ID = 8;
const FILAS_ESPERADAS = 20; // 10 Films + 10 TV

export interface NetflixRow {
  week: string;            // YYYY-MM-DD
  category: MediaType;
  rank: number;
  rawTitle: string;
}

export interface StoredRow {
  rank: number;
  rawTitle: string;
  tmdbId: number | null;
}

// Minúsculas, sin acentos, sin puntuación, espacios colapsados. Se usa para
// comparar el título del TSV contra el de TMDB: "Fear" != "State of Fear".
export function normalizeTitle(s: string): string {
  return s
    .normalize("NFD")
    .replace(/[̀-ͯ]/g, "")
    .toLowerCase()
    .replace(/[^a-z0-9\s]/g, " ")
    .replace(/\s+/g, " ")
    .trim();
}

// Toma las primeras líneas del TSV y devuelve las filas de AR de la semana más
// reciente. Tira si el archivo no arranca como se espera: preferimos no
// escribir nada antes que escribir basura si Netflix cambia el orden.
export function parseArWeek(lines: string[]): NetflixRow[] {
  const [header, ...body] = lines;
  if (!header?.startsWith("country_name\tcountry_iso2\tweek\tcategory\tweekly_rank")) {
    throw new Error("El encabezado del TSV cambió");
  }
  const out: NetflixRow[] = [];
  let week: string | null = null;
  for (const line of body) {
    const c = line.split("\t");
    if (c.length < 6) continue;
    const [, iso2, w, category, rank, rawTitle] = c;
    if (iso2 !== "AR") break;
    if (week === null) week = w;
    if (w !== week) break;
    if (category !== "Films" && category !== "TV") {
      throw new Error(`Categoría inesperada en el TSV: ${category}`);
    }
    out.push({
      week: w,
      category: category === "Films" ? "movie" : "tv",
      rank: Number(rank),
      rawTitle,
    });
  }
  if (out.length !== FILAS_ESPERADAS) {
    throw new Error(`Se esperaban ${FILAS_ESPERADAS} filas de AR, llegaron ${out.length}`);
  }
  return out;
}

// Lee el TSV cortando el stream apenas tiene las filas que necesita.
async function fetchArWeek(): Promise<NetflixRow[]> {
  const res = await fetch(TSV_URL, { cache: "no-store", signal: AbortSignal.timeout(20000) });
  if (!res.ok || !res.body) throw new Error(`TSV respondió ${res.status}`);
  const reader = res.body.getReader();
  const dec = new TextDecoder();
  const lines: string[] = [];
  let buf = "";
  try {
    // +2: el encabezado y una línea de margen para detectar el corte de país.
    while (lines.length < FILAS_ESPERADAS + 2) {
      const { done, value } = await reader.read();
      if (done) break;
      buf += dec.decode(value, { stream: true });
      const parts = buf.split("\n");
      buf = parts.pop() ?? "";
      lines.push(...parts);
    }
  } finally {
    await reader.cancel().catch(() => {});
  }
  return parseArWeek(lines);
}

// Resuelve un título del TSV a un id de TMDB.
//
// Reglas, en orden (medidas contra la semana 2026-08-02: 19/20 automáticos):
//  1. título exacto Y está en Netflix AR -> se acepta
//  2. título exacto, no figura en Netflix AR -> se acepta igual (lag de TMDB en
//     estrenos recientes; le pasó a "My Daughter's Father")
//  3. título distinto pero está en Netflix AR -> se acepta con needs_review
//     ("Fear" matcheó "State of Fear")
//  4. sin candidatos -> null + needs_review
async function resolveTitle(
  category: MediaType, rawTitle: string,
): Promise<{ tmdbId: number | null; needsReview: boolean }> {
  const objetivo = normalizeTitle(rawTitle);
  let candidatos: { id: number; title: string }[] = [];
  try {
    const r = await searchTitles(category, rawTitle);
    candidatos = (r.results ?? []).slice(0, 5).map((c) => ({
      id: c.id, title: c.title ?? c.name ?? "",
    }));
  } catch {
    return { tmdbId: null, needsReview: true };
  }
  if (!candidatos.length) return { tmdbId: null, needsReview: true };

  const exacto = candidatos.find((c) => normalizeTitle(c.title) === objetivo);
  if (exacto) return { tmdbId: exacto.id, needsReview: false };

  for (const c of candidatos) {
    if (await enNetflixAR(category, c.id)) return { tmdbId: c.id, needsReview: true };
  }
  return { tmdbId: candidatos[0].id, needsReview: true };
}

async function enNetflixAR(type: MediaType, id: number): Promise<boolean> {
  try {
    const r = await watchProviders(type, id);
    return (r.results?.AR?.flatrate ?? []).some((p) => p.provider_id === NETFLIX_PROVIDER_ID);
  } catch {
    return false;
  }
}

export async function ingestLatestWeek() {
  const db = supabaseServer();
  if (!db) throw new Error("Supabase no configurado (falta SUPABASE_SERVICE_ROLE_KEY)");

  const filas = await fetchArWeek();
  const week = filas[0].week;

  // Lo que ya está guardado para esa semana: si el raw_title no cambió, la fila
  // NO se toca. Así una corrección manual de tmdb_id sobrevive a los reintentos.
  const { data: previas } = await db
    .from("netflix_top10")
    .select("category,rank,raw_title")
    .eq("week", week);
  const yaEsta = new Map(
    (previas ?? []).map((p) => [`${p.category}:${p.rank}`, p.raw_title as string]),
  );

  const pendientes = filas.filter(
    (f) => yaEsta.get(`${f.category}:${f.rank}`) !== f.rawTitle,
  );
  if (!pendientes.length) return { week, inserted: 0, resolved: 0, review: 0 };

  // Mapa de títulos ya resueltos en semanas anteriores, acotado por categoría:
  // un mismo nombre puede existir como película y como serie.
  const { data: conocidos } = await db
    .from("netflix_top10")
    .select("raw_title,category,tmdb_id")
    .in("raw_title", pendientes.map((f) => f.rawTitle))
    .not("tmdb_id", "is", null);
  const mapa = new Map(
    (conocidos ?? []).map((c) => [`${c.category}:${c.raw_title}`, c.tmdb_id as number]),
  );

  const rows = [];
  let resolved = 0, review = 0;
  for (const f of pendientes) {
    const conocido = mapa.get(`${f.category}:${f.rawTitle}`);
    const r = conocido != null
      ? { tmdbId: conocido, needsReview: false }
      : await resolveTitle(f.category, f.rawTitle);
    if (r.tmdbId != null) resolved++;
    if (r.needsReview) review++;
    rows.push({
      week: f.week, category: f.category, rank: f.rank, raw_title: f.rawTitle,
      tmdb_id: r.tmdbId, needs_review: r.needsReview, updated_at: new Date().toISOString(),
    });
  }

  const { error } = await db.from("netflix_top10").upsert(rows, {
    onConflict: "week,category,rank",
  });
  if (error) throw new Error(`upsert falló: ${error.message}`);
  return { week, inserted: rows.length, resolved, review };
}

// Devuelve la semana más reciente guardada. null si la tabla está vacía o si
// Supabase no está configurado: el llamador cae a popularidad.
export async function latestWeekRows(): Promise<
  { week: string; movie: StoredRow[]; tv: StoredRow[] } | null
> {
  const db = supabaseServer();
  if (!db) return null;
  const { data: ultima } = await db
    .from("netflix_top10")
    .select("week")
    .order("week", { ascending: false })
    .limit(1)
    .maybeSingle();
  if (!ultima?.week) return null;
  const { data } = await db
    .from("netflix_top10")
    .select("category,rank,raw_title,tmdb_id")
    .eq("week", ultima.week)
    .order("rank");
  if (!data?.length) return null;
  const pick = (cat: MediaType): StoredRow[] => data
    .filter((r) => r.category === cat)
    .map((r) => ({ rank: r.rank as number, rawTitle: r.raw_title as string, tmdbId: r.tmdb_id as number | null }));
  return { week: ultima.week as string, movie: pick("movie"), tv: pick("tv") };
}
```

- [ ] **Step 3: Verificar que compila**

Run: `npx tsc --noEmit`
Expected: sin salida.

- [ ] **Step 4: Commit**

```bash
git add lib/tmdb.ts lib/netflix-top10.ts
git commit -m "feat(top): ingesta y resolución del top 10 de Netflix AR"
```

---

## Task 4: Cron de ingesta

**Files:**
- Create: `app/api/cron/netflix-top10/route.ts`
- Create: `vercel.json`

**Interfaces:**
- Consumes: `ingestLatestWeek()` de `lib/netflix-top10.ts`.
- Produces: `GET /api/cron/netflix-top10` que devuelve `{ ok: true, week, inserted, resolved, review }` o `{ ok: false, error }` con status 500.

- [ ] **Step 1: Crear el handler**

`app/api/cron/netflix-top10/route.ts`:

```ts
import { NextRequest, NextResponse } from "next/server";
import { ingestLatestWeek } from "@/lib/netflix-top10";

export const dynamic = "force-dynamic";
// Resolver títulos nuevos son 1-2 requests a TMDB cada uno; con 20 títulos
// nuevos (la primera corrida) puede pasarse de los 10s del default.
export const maxDuration = 60;

export async function GET(req: NextRequest) {
  // Vercel Cron manda `Authorization: Bearer $CRON_SECRET`. Sin el secreto
  // configurado, la ruta queda cerrada: mejor no ingestar que dejarla abierta.
  const secret = process.env.CRON_SECRET;
  if (!secret || req.headers.get("authorization") !== `Bearer ${secret}`) {
    return NextResponse.json({ ok: false, error: "no autorizado" }, { status: 401 });
  }
  try {
    return NextResponse.json({ ok: true, ...(await ingestLatestWeek()) });
  } catch (e) {
    return NextResponse.json({ ok: false, error: String(e) }, { status: 500 });
  }
}
```

- [ ] **Step 2: Crear `vercel.json`**

El archivo no existe todavía. Martes 12:00 UTC = 09:00 en Argentina, unas horas después de que Netflix publica.

```json
{
  "crons": [
    { "path": "/api/cron/netflix-top10", "schedule": "0 12 * * 2" }
  ]
}
```

- [ ] **Step 3: Definir `CRON_SECRET`**

En `.env.local` (y en las variables de entorno del proyecto en Vercel, que lo inyecta solo en los requests del cron):

```
CRON_SECRET=<cualquier string largo y aleatorio>
```

Agregar la línea también a `.env.local.example`, sin valor.

- [ ] **Step 4: Correr el cron a mano y verificar**

Con `npm run dev` levantado:

```bash
curl -s -H "Authorization: Bearer $CRON_SECRET" http://localhost:3000/api/cron/netflix-top10
```

Expected (primera corrida): `{"ok":true,"week":"2026-08-02","inserted":20,"resolved":20,"review":1}`. `review` puede ser 0, 1 o 2 según la semana. `resolved` debería ser 19 o 20.

Verificar sin el header:

```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/api/cron/netflix-top10
```

Expected: `401`.

- [ ] **Step 5: Verificar la idempotencia**

Correr el mismo comando del Step 4 una segunda vez.

Expected: `{"ok":true,"week":"...","inserted":0,"resolved":0,"review":0}` — ninguna fila tocada, porque los `raw_title` no cambiaron.

- [ ] **Step 6: Revisar a ojo lo que quedó guardado**

En el SQL editor de Supabase:

```sql
select category, rank, raw_title, tmdb_id, needs_review
from netflix_top10 order by category, rank;
```

Expected: 20 filas. Las marcadas `needs_review = true` son las que hay que mirar (esperable: 1 o 2). Corregir a mano con `update netflix_top10 set tmdb_id = <id>, needs_review = false where week = '...' and category = '...' and rank = ...;`

- [ ] **Step 7: Commit**

```bash
git add app/api/cron/netflix-top10/route.ts vercel.json .env.local.example
git commit -m "feat(top): cron semanal de ingesta del top 10 de Netflix"
```

---

## Task 5: `lib/top.ts` y `/api/top`

**Files:**
- Create: `lib/top.ts`
- Create: `app/api/top/route.ts`

**Interfaces:**
- Consumes: `latestWeekRows()` de `lib/netflix-top10.ts`; `cardsByIds`, `listByCategory` de `lib/enrich.ts`; `cached`, `TTL` de `lib/cache.ts`; `PLATFORMS` de `lib/providers-ar.ts`.
- Produces:
  - `TOP_PLATFORMS: PlatformCode[]` = `["n", "p", "m", "d", "at", "cr"]`
  - `type TopSlot = { rank: number; item: UITitle | null; rawTitle: string }`
  - `type TopBlock = { platform: PlatformCode; source: "netflix" | "popular"; week?: string; slots: TopSlot[] }`
  - `buildTop(tipo: MediaType, providers: PlatformCode[]): Promise<{ mine: TopBlock[]; others: TopBlock[] }>`

- [ ] **Step 1: Crear `lib/top.ts`**

```ts
import "server-only";
import { cardsByIds, listByCategory } from "./enrich";
import { latestWeekRows } from "./netflix-top10";
import { cached, TTL } from "./cache";
import { platformOrder } from "./providers-ar";
import type { MediaType, PlatformCode, UITitle } from "./types";

// Las seis del v1. El orden es el de aparición en la página dentro de cada
// grupo. Elegidas por tamaño de catálogo en AR (ver el spec): las demás no
// llegan a diez posiciones sin verse vacías al lado de Netflix.
export const TOP_PLATFORMS: PlatformCode[] = ["n", "p", "m", "d", "at", "cr"];

const TOPE = 10;

// `item` en null = la posición existe pero no tenemos ficha (título de Netflix
// sin resolver). Se renderiza igual: un top 9 se lee como un bug.
export interface TopSlot { rank: number; item: UITitle | null; rawTitle: string }
export interface TopBlock {
  platform: PlatformCode;
  source: "netflix" | "popular";
  week?: string;
  slots: TopSlot[];
}

// Bloque por popularidad. Sin `scope`, así que NO corren los filtros de
// animación/familia de lib/audience.ts: esto es un ranking, no un riel curado.
async function popularBlock(platform: PlatformCode, tipo: MediaType): Promise<TopBlock> {
  const items = await cached(`top:pop:${platform}:${tipo}`, TTL.catalog, () =>
    listByCategory({ tipo, providers: [platform], sortBy: "popularity.desc" }),
  );
  return {
    platform,
    source: "popular",
    slots: items.slice(0, TOPE).map((item, i) => ({
      rank: i + 1, item, rawTitle: item.title,
    })),
  };
}

// Bloque de Netflix con el dato oficial. Si la tabla está vacía (el cron nunca
// corrió, o Supabase no está configurado) cae a popularidad: nunca se muestra
// un bloque roto ni un hueco.
async function netflixBlock(tipo: MediaType): Promise<TopBlock> {
  const data = await latestWeekRows();
  const filas = tipo === "movie" ? data?.movie : data?.tv;
  if (!data || !filas?.length) return popularBlock("n", tipo);

  const conId = filas.filter((f) => f.tmdbId != null);
  const cards = await cardsByIds(conId.map((f) => ({ tipo, id: f.tmdbId as number })));
  // cardsByIds descarta lo que no pudo enriquecer y no preserva el orden, así
  // que se reindexa por id y se rearma siguiendo el rank, que es el que manda.
  const porId = new Map(cards.map((c) => [c.id, c]));

  return {
    platform: "n",
    source: "netflix",
    week: data.week,
    slots: filas.slice(0, TOPE).map((f) => ({
      rank: f.rank,
      item: f.tmdbId != null ? porId.get(f.tmdbId) ?? null : null,
      rawTitle: f.rawTitle,
    })),
  };
}

export async function buildTop(tipo: MediaType, providers: PlatformCode[]) {
  const elegidas = new Set(providers);
  const orden = (a: PlatformCode, b: PlatformCode) => platformOrder(a) - platformOrder(b);
  const mias = TOP_PLATFORMS.filter((p) => elegidas.has(p)).sort(orden);
  const resto = TOP_PLATFORMS.filter((p) => !elegidas.has(p)).sort(orden);

  const armar = (p: PlatformCode) => (p === "n" ? netflixBlock(tipo) : popularBlock(p, tipo));
  // Sin limitador propio: el semáforo de lib/tmdb.ts ya pone el techo global.
  const [mine, others] = await Promise.all([
    Promise.all(mias.map(armar)),
    Promise.all(resto.map(armar)),
  ]);
  // Un bloque sin nada que mostrar no se renderiza.
  const conAlgo = (b: TopBlock) => b.slots.length > 0;
  return { mine: mine.filter(conAlgo), others: others.filter(conAlgo) };
}
```

- [ ] **Step 2: Crear `app/api/top/route.ts`**

```ts
import { NextRequest, NextResponse } from "next/server";
import { buildTop } from "@/lib/top";
import type { MediaType, PlatformCode } from "@/lib/types";

export const dynamic = "force-dynamic";
export const maxDuration = 60;

export async function GET(req: NextRequest) {
  const q = req.nextUrl.searchParams;
  const tipo: MediaType = q.get("tipo") === "tv" ? "tv" : "movie";
  const providers = (q.get("providers") || "")
    .split(",").map((s) => s.trim()).filter(Boolean) as PlatformCode[];
  try {
    return NextResponse.json(await buildTop(tipo, providers));
  } catch (e) {
    return NextResponse.json({ error: String(e), mine: [], others: [] }, { status: 500 });
  }
}
```

- [ ] **Step 3: Verificar que compila**

Run: `npx tsc --noEmit`
Expected: sin salida.

- [ ] **Step 4: Probar el endpoint**

Con `npm run dev`:

```bash
curl -s "http://localhost:3000/api/top?tipo=movie&providers=n,d,m" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const f=b=>b.platform+':'+b.source+':'+b.slots.length+' llenos='+b.slots.filter(x=>x.item).length;console.log('mine ',j.mine.map(f).join('  '));console.log('others',j.others.map(f).join('  '))})"
```

Expected, algo como:

```
mine  n:netflix:10 llenos=10  d:popular:10 llenos=10  m:popular:10 llenos=10
others p:popular:10 llenos=10  at:popular:10 llenos=10  cr:popular:10 llenos=10
```

Lo que importa: Netflix sale con `source: netflix`, las otras cinco con `popular`, los bloques del usuario van en `mine` y el resto en `others`.

- [ ] **Step 5: Probar el fallback de Netflix**

En Supabase: `delete from netflix_top10;`, después repetir el curl del Step 4.

Expected: `n:popular:10` — Netflix cayó a popularidad sin romper nada.

Volver a correr el cron (Task 4, Step 4) para dejar la tabla como estaba.

- [ ] **Step 6: Commit**

```bash
git add lib/top.ts app/api/top/route.ts
git commit -m "feat(top): composer de bloques y endpoint /api/top"
```

---

## Task 6: Número de ranking en la card

**Files:**
- Modify: `components/TitleCard.tsx`
- Modify: `app/globals.css`

**Interfaces:**
- Consumes: nada.
- Produces: `TitleCard` acepta `rank?: number`. Sin la prop, el render es idéntico al de hoy.

- [ ] **Step 1: Agregar la prop a `TitleCard`**

En `components/TitleCard.tsx`, cambiar la firma:

```tsx
export default function TitleCard({ t, rank }: { t: UITitle; rank?: number }) {
```

y dentro del `<div className="poster">`, agregar el número después del `ed-flag` y antes del `ptitle`:

```tsx
        <div className="poster" style={bg}>
          {t.hasEditorial && <div className="ed-flag">{star}Reseña SC</div>}
          {rank != null && <span className={`rank-num${rank <= 3 ? " top3" : ""}`}>{rank}</span>}
          {!t.poster && <div className="ptitle rank-shift">{t.title}</div>}
        </div>
```

El `rank-shift` corre el título cuando no hay póster: sin eso el número y el título se pisan en el mismo rincón.

- [ ] **Step 2: Agregar el CSS**

En `app/globals.css`, después del bloque de `.ed-flag` (línea ~209):

```css
/* Número de ranking del Top. Va DENTRO del póster, abajo a la izquierda, sobre
   el degradado que .poster::after ya dibuja. Se descartó el número gigante al
   costado (estilo Netflix) porque obliga a ensanchar la card de 158px a ~230px
   y el riel dejaría de calzar con el resto de la app. */
.rank-num{position:absolute;left:10px;bottom:6px;z-index:3;font-family:var(--display);font-size:46px;font-weight:800;line-height:.8;color:#fff;text-shadow:0 2px 16px rgba(0,0,0,.7);pointer-events:none}
.rank-num.top3{color:var(--accent)}
.ptitle.rank-shift{padding-left:38px}
```

- [ ] **Step 3: Verificar que compila y que nada se rompió**

Run: `npx tsc --noEmit`
Expected: sin salida.

Con `npm run dev`, abrir el Home: las cards no deben mostrar ningún número (nadie pasa `rank` todavía) y verse exactamente igual que antes.

- [ ] **Step 4: Commit**

```bash
git add components/TitleCard.tsx app/globals.css
git commit -m "feat(top): prop rank opcional en TitleCard"
```

---

## Task 7: La sección `/top` y su acceso en el nav

**Files:**
- Create: `components/TopView.tsx`
- Create: `app/top/page.tsx`
- Modify: `components/BottomNav.tsx`
- Modify: `app/globals.css`

**Interfaces:**
- Consumes: `TopBlock`/`TopSlot` de `lib/top.ts`; `useApi`, `usePlatforms`, `useShelfType`, `ShelfTypeToggle`, `TitleCard`, `PlatformLogo`, `OfflineState`.
- Produces: la ruta `/top`.

- [ ] **Step 1: Crear `components/TopView.tsx`**

```tsx
"use client";
import { useRef, useState } from "react";
import { useApi } from "./useApi";
import { usePlatforms } from "./PlatformsContext";
import { useShelfType } from "@/hooks/useShelfType";
import ShelfTypeToggle from "./ShelfTypeToggle";
import TitleCard from "./TitleCard";
import PlatformLogo from "./PlatformLogo";
import OfflineState from "./pwa/OfflineState";
import { platformByCode } from "@/lib/providers-ar";
import type { TopBlock } from "@/lib/top";

const NOTA =
  "El top de Netflix sale de los datos que la propia Netflix publica cada semana " +
  "para Argentina: son horas vistas reales, de la semana que cerró el domingo. " +
  "En el resto de las plataformas no hay dato público de consumo, así que " +
  "mostramos las más populares del momento — no es lo mismo, y por eso lo " +
  "aclaramos.";

function Bloque({ b }: { b: TopBlock }) {
  const track = useRef<HTMLDivElement>(null);
  const [nota, setNota] = useState(false);
  const def = platformByCode(b.platform);
  const scroll = (d: number) =>
    track.current?.scrollBy({ left: d * (track.current.clientWidth * 0.8), behavior: "smooth" });

  return (
    <div className="shelf">
      <div className="shelf-head">
        <div className="shelf-head-l">
          {/* El wordmark de PlatformLogo YA es el nombre de la plataforma en su
              tipografía de marca: poner además `def.name` al lado lo duplica
              ("NETFLIX Netflix"). El aria-label deja el nombre para lectores. */}
          <h2 aria-label={def?.name ?? b.platform}><PlatformLogo code={b.platform} /></h2>
          {b.source === "netflix" && (
            <button
              type="button" className="top-info" aria-label="De dónde sale este dato"
              aria-expanded={nota} onClick={() => setNota((v) => !v)}
            >i</button>
          )}
        </div>
        <div className="arrows">
          <button className="arrow" onClick={() => scroll(-1)} aria-label="Anterior"><svg viewBox="0 0 24 24"><path d="M15 18l-6-6 6-6" /></svg></button>
          <button className="arrow" onClick={() => scroll(1)} aria-label="Siguiente"><svg viewBox="0 0 24 24"><path d="M9 18l6-6-6-6" /></svg></button>
        </div>
      </div>
      <p className="top-sub">
        {b.source === "netflix" ? "Lo más visto esta semana · dato oficial" : "Lo más popular ahora"}
      </p>
      {nota && <p className="top-nota">{NOTA}</p>}
      <div className="track" ref={track}>
        {b.slots.map((s) =>
          s.item
            ? <TitleCard key={`${s.rank}-${s.item.id}`} t={s.item} rank={s.rank} />
            : (
              <div className="card rank-slot" key={`vacio-${s.rank}`}>
                <div className="poster">
                  <span className="rank-num">{s.rank}</span>
                  <div className="ptitle rank-shift">{s.rawTitle}</div>
                </div>
                <div className="meta"><div className="info">Ficha no disponible</div></div>
              </div>
            ),
        )}
      </div>
    </div>
  );
}

export default function TopView() {
  const { platforms } = usePlatforms();
  const [tipo, setTipo] = useShelfType("top", "movie");
  const { data, loading, offline, retry } = useApi<{ mine: TopBlock[]; others: TopBlock[] }>(
    () => `/api/top?tipo=${tipo}&providers=${platforms.join(",")}`,
    [tipo, platforms.join(",")],
    { keepPrevious: true },
  );

  if (offline) return <div className="wrap"><OfflineState onRetry={retry} /></div>;

  const mine = data?.mine ?? [];
  const others = data?.others ?? [];

  return (
    <div className="wrap">
      <h1 className="section-title">Top Yump</h1>
      <p className="section-sub">Lo que más se está viendo en Argentina.</p>
      <div className="top-toggle">
        <ShelfTypeToggle value={tipo} onChange={setTipo} />
      </div>

      {loading && !data ? <div className="loading">Cargando…</div> : (
        <>
          {mine.length > 0 && (
            <>
              <span className="chip-group-label">Tus plataformas</span>
              {mine.map((b) => <Bloque key={b.platform} b={b} />)}
            </>
          )}
          {others.length > 0 && (
            <>
              <span className="chip-group-label">En otras plataformas</span>
              {others.map((b) => <Bloque key={b.platform} b={b} />)}
            </>
          )}
        </>
      )}
    </div>
  );
}
```

Las firmas usadas están verificadas contra el código actual: `useApi<T>(url, deps, options)` con `options.keepPrevious` y `retry` en el retorno (`components/useApi.ts:40`), y `OfflineState({ onRetry })` (`components/pwa/OfflineState.tsx:8`), envuelto en `.wrap` igual que en `CatalogView.tsx:56`.

- [ ] **Step 2: Crear `app/top/page.tsx`**

Copiar el patrón de una página existente equivalente (`app/directores/page.tsx` o `app/proximamente/page.tsx`), que es un wrapper mínimo:

```tsx
import TopView from "@/components/TopView";

export const metadata = { title: "Top Yump" };

export default function Page() {
  return <TopView />;
}
```

- [ ] **Step 3: Agregar el CSS**

En `app/globals.css`, junto al resto de lo del Top:

```css
/* Sección Top. El toggle es uno solo para toda la página (a diferencia del
   Home, donde va uno por riel): los bloques son la misma pregunta repetida por
   plataforma, y estados independientes rompen la lectura de "el top de ahora". */
.top-toggle{position:sticky;top:0;z-index:20;padding:10px 0 14px;background:rgba(var(--bg-rgb),.92);backdrop-filter:blur(14px)}
.top-sub{color:var(--dim);font-size:13px;margin:-8px 0 12px}
.top-nota{color:var(--dim);font-size:13px;line-height:1.5;margin:0 0 14px;padding:12px 14px;border:1px solid var(--line);border-radius:var(--radius-sm);background:var(--surface)}
.top-info{width:20px;height:20px;border-radius:50%;border:1px solid var(--line-2);background:var(--surface);color:var(--dim);font-family:var(--display);font-size:12px;font-weight:700;line-height:1;cursor:pointer;display:inline-flex;align-items:center;justify-content:center}
.top-info:hover{border-color:var(--accent);color:var(--accent)}
.top-info:focus-visible{outline:2px solid var(--accent);outline-offset:2px}
/* Posición sin ficha: ocupa el lugar igual. Un top 9 se lee como un bug. */
.rank-slot .poster{background:var(--surface-2);color:var(--text)}
.rank-slot{flex:0 0 158px}
```

- [ ] **Step 4: Agregar el quinto ítem al nav**

En `components/BottomNav.tsx`, insertar entre Inicio y Buscador:

```tsx
  { href: "/top", label: "Top", match: (p: string) => p.startsWith("/top"), icon: <><path d="M6 20V10" /><path d="M12 20V4" /><path d="M18 20v-7" /></> },
```

No hay que tocar `.bottomnav`: `.navitem` usa `flex:1` y reparte solo. La etiqueta es corta a propósito — a 12px, en cinco columnas y en un viewport de 320px, cada celda queda en 64px.

- [ ] **Step 5: Verificar**

Run: `npx tsc --noEmit`
Expected: sin salida.

Con `npm run dev`, en `http://localhost:3000/top`:

- El nav inferior muestra cinco ítems y "Top" queda activo.
- Los bloques de las plataformas elegidas aparecen bajo "Tus plataformas" y el resto bajo "En otras plataformas".
- Netflix dice "Lo más visto esta semana · dato oficial" y tiene el `ⓘ`; las demás dicen "Lo más popular ahora".
- Los números 1, 2 y 3 salen en coral y el resto en blanco.
- Al pasar el toggle a Series siguen apareciendo las seis plataformas.
- Recargar mantiene la elección del toggle (queda en `localStorage`, key `yump:shelf-type`).

Probar también en 320px de ancho (DevTools) que las cinco etiquetas del nav no se corten.

- [ ] **Step 6: Build completo**

Run: `npx next build`
Expected: compila. Es el paso que caza el arrastre de clientes server-only al bundle del navegador, que `tsc` no ve. Si falla solo por la descarga de Google Fonts (sandbox sin red), no es un error real.

- [ ] **Step 7: Commit**

```bash
git add components/TopView.tsx app/top/page.tsx components/BottomNav.tsx app/globals.css
git commit -m "feat(top): sección /top y acceso en el nav inferior"
```

---

## Task 8: Documentación

**Files:**
- Modify: `CLAUDE.md`

**Interfaces:**
- Consumes: nada.
- Produces: nada.

- [ ] **Step 1: Actualizar `CLAUDE.md`**

En la tabla de rutas API, agregar dos filas:

```markdown
| `GET /api/top` | top 10 por plataforma (Netflix real + popularidad), vía `lib/top.ts` |
| `GET /api/cron/netflix-top10` | ingesta semanal del TSV oficial de Netflix (Vercel Cron, martes) |
```

En la sección "Decisiones de arquitectura que importan", agregar:

```markdown
- **El Top (`/top`) es la única sección con dato de consumo real, y solo para
  Netflix.** Sale del TSV público de Netflix (`lib/netflix-top10.ts`), ingestado
  por un cron semanal a la tabla `netflix_top10`, que hace de ranking **y** de
  mapa título→TMDB (el TSV solo trae el título en inglés). Las otras cinco
  plataformas van por popularidad de TMDB y se etiquetan distinto a propósito:
  "Lo más popular ahora" vs "Lo más visto esta semana · dato oficial". No unificar
  ese copy — la distinción es deliberada. Disney+, Prime Video, Max, Paramount+ y
  Apple TV+ **no publican** su top 10; las únicas fuentes que lo agregan
  (FlixPatrol, JustWatch) son de pago. En el Top no corren ni los filtros de
  audiencia ni el dedup del Home.
```

En "Estructura", agregar `netflix-top10.ts` y `top.ts` al listado de `lib/`.

En la sección de plataformas, dejar asentado el arreglo de la Tarea 1: el id 2 de Apple TV+ era la tienda de alquiler, y Star+ se fusionó con Disney+.

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: documentar la sección Top en CLAUDE.md"
```

---

## Autorrevisión del plan

**Cobertura del spec:**

| Requisito del spec | Tarea |
|---|---|
| Sacar el id 2 de Apple TV+ y Star+ | 1 |
| Tabla `netflix_top10` + RLS | 2 |
| Corte del stream del TSV en la línea 21 | 3 (`fetchArWeek`) |
| Validar orden del archivo y abortar sin escribir | 3 (`parseArWeek` tira) |
| Las 4 reglas de resolución de títulos | 3 (`resolveTitle`) |
| Lookup del mapa acotado por categoría | 3 (clave `category:rawTitle`) |
| Upsert que no pisa correcciones manuales | 3 (filtro `pendientes`) + 4 Step 5 |
| Cron martes 09:00 AR con `CRON_SECRET` | 4 |
| `GET /api/top` con `{ mine, others }` | 5 |
| Netflix desde `max(week)` | 3 (`latestWeekRows`) |
| Popularidad 24 h en Redis | 5 (`cached` con `TTL.catalog`) |
| Tope de concurrencia | Global Constraints — ya está en `lib/tmdb.ts` |
| Sin filtros de audiencia ni dedup | 5 (no se pasa `scope`) |
| Nav: quinto ítem "Top" | 7 Step 4 |
| Toggle global sticky | 7 (`.top-toggle` + `useShelfType("top")`) |
| Copy diferenciado + `ⓘ` | 7 (`Bloque`) |
| `.rank-num` dentro del póster, top 3 en coral | 6 |
| `rank?: number` en `TitleCard` | 6 |
| Fallback de Netflix a popularidad | 5 (`netflixBlock`) + 5 Step 5 |
| Slot sin resolver con card neutra | 5 (`item: null`) + 7 (`.rank-slot`) |
| Offline | 7 (`OfflineState`) |
| Ninguna plataforma se oculta en Series | 7 Step 5 |
| Bloque con menos de 10 ítems | 5 (`slice`, sin relleno) |

Sin huecos.

**Consistencia de tipos:** `NetflixRow.category` y `TopSlot`/`TopBlock` usan `MediaType` (`"movie" | "tv"`) de punta a punta; la conversión desde `Films`/`TV` del TSV pasa una sola vez, en `parseArWeek`. `latestWeekRows()` devuelve `StoredRow[]` con `tmdbId` camelCase, mientras las columnas son `snake_case` — la traducción está en el `map` de esa misma función y en ningún otro lado.

**Firmas verificadas contra el código actual** (no asumidas): `useApi` (`components/useApi.ts:40`), `OfflineState` (`components/pwa/OfflineState.tsx:8`), `cardsByIds` y `listByCategory` (`lib/enrich.ts:652` y `:104`), `watchProviders` y el semáforo `MAX_EN_VUELO` (`lib/tmdb.ts:147` y `:28`), `cached`/`TTL` (`lib/cache.ts:64` y `:71`), `PlatformLogo` (devuelve un `<span>` wordmark, no un `<img>`).

**Riesgo conocido:** `discover` fija `vote_count.gte` en 60 por defecto (`lib/tmdb.ts:117`). Para los bloques de popularidad eso es deseable — saca ruido sin votos — pero significa que un estreno muy reciente y muy popular puede no aparecer en el top de una plataforma chica. Si al revisar la Tarea 5 se ve que falta algo obvio, bajarlo pasando `minVotes` en `listByCategory`.
