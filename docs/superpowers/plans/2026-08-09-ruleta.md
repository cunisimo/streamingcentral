# Ruleta "no sé qué ver" — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un banner en el Home que, tras elegir una de cuatro situaciones, devuelve **una** recomendación de película con su razón, su advertencia cuando la hay, y dónde verla.

**Architecture:** La función `get_roulette_picks` de Supabase (ya existe) devuelve 20 candidatos por tanda; "Otra" los consume del lado del cliente sin volver a consultar. Un endpoint fino traduce los códigos de plataforma del usuario a los nombres que guarda `title_availability`, llama a la función y enriquece los títulos con la misma función cacheada en Redis que usan los rieles del Home.

**Tech Stack:** Next.js 14 App Router, TypeScript, Supabase (RPC + RLS), Upstash Redis, TMDB.

**Spec:** `docs/superpowers/specs/2026-08-09-ruleta-design.md`

## Global Constraints

### Alcance: qué NO se toca (es el riesgo principal de esta tarea)

- **No se tocan los 16 chips del Home** ni sus definiciones en `lib/categories.ts`, ni `navidad`, ni los rieles del Home.
- **No se tocan `chip_titles` ni `chip_blocklist`.**
- **`title_availability` es de sólo lectura** desde la app. La escribe únicamente el pipeline offline.
- **No se toca nada en `scripts/` ni en `prompts/`.**
- **No se modifica `roulette_titles` ni `get_roulette_picks`**: ya existen y funcionan.

Si para implementar esto hiciera falta modificar algo del Home o de los chips, **parar y avisar antes de hacerlo**.

### Reglas de producto

- Todo el texto de la UI en español rioplatense.
- Sin CSS-in-JS: todo en `app/globals.css`, reusando clases existentes antes de inventar una nueva.
- **Una sola opción a la vez.** Nunca una lista: reconstruiría el problema que la feature resuelve.
- **Una query por tanda**, no una por toque. "Otra" consume de los 20 que ya están en el cliente.
- **`atencion` nunca se muestra crudo** (`alta`/`media`/`fondo` son dato, no copy). Se muestra una frase del banco, que vive en el código.
- **`advertencia` NULL → el bloque "PERO" no se renderiza.** Sin relleno ni placeholder.
- **Para mostrar, TMDB.** `roulette_titles.title` es un snapshot de auditoría, no se muestra.
- Las rutas API son finas: parsean y llaman a una función de `lib/`.
- `lib/tmdb.ts` ya tiene un semáforo global de concurrencia (`MAX_EN_VUELO`, 24). **No agregar un limitador propio.**
- Los `lib/` que tocan datos llevan `import "server-only"`.

### Verificación

**El proyecto no tiene runner de tests**, por decisión del dueño. El ciclo de verificación es `npx tsc --noEmit`, `npx next build` y comprobaciones manuales con salida esperada. Cada tarea cierra con comandos concretos, no con tests automatizados.

---

## Datos verificados contra la base (2026-08-09)

No los re-investigues; están medidos.

- `get_roulette_picks(p_providers text[], p_escenario text, p_excluir integer[], p_region text, p_seed text, p_limit integer)` existe y devuelve 20 filas en los cuatro escenarios pasando sólo `['Netflix','Disney Plus','HBO Max']`.
- Columnas que devuelve: `tmdb_id, media_type, title, year, runtime, genres, edad, razon, advertencia, atencion, vote_average, providers`.
- `roulette_titles`: 1811 filas, **765 con `razon`**. De esas, **696 son alcanzables** con las 14 plataformas que mapea la app. Por atención: 193 `alta`, 380 `media`, 123 `fondo`; 112 con `apto_chicos`.
- En `title_availability` hay **31 nombres distintos** de proveedor, no los 13 que enumera el spec.
- **`"VIX "` viene con un espacio al final.** Es dato del pipeline; la app lo mapea tal cual, no lo corrige.
- `"Apple TV"` en esta tabla **es la suscripción**, no la tienda: la columna sólo guarda flatrate/free/ads y excluye alquiler por diseño. Es un nombre distinto del `provider_id` 2 de TMDB, que sí era la tienda y se sacó de `lib/providers-ar.ts` en otra rama.
- `providersOf` (`lib/enrich.ts:45`) ya cachea `watchLink` bajo `pv:${type}:${id}`, la misma clave que calienta `cardsByIds`. El deep link del spec (punto 8) no cuesta un request extra.
- "Ya la vi" de la ficha: `components/ListActions.tsx:30` → `if (!user) { router.push("/cuenta"); return; }`, y luego `setItem(user.id, "watched", ...)` optimista con rollback.
- La semilla del día es `dailySeed()` (`lib/cache.ts:88`), devuelve `number`.
- `useAuth()` expone `{ user, ready }`.

---

## Estructura de archivos

| Archivo | Responsabilidad |
|---|---|
| `lib/roulette-providers.ts` (crear) | mapa `PlatformCode` → nombres de `title_availability`, con guard ruidoso |
| `lib/roulette.ts` (crear) | llama a la RPC, enriquece con TMDB, arma el pick |
| `app/api/ruleta/route.ts` (crear) | ruta fina |
| `hooks/useRouletteSeen.ts` (crear) | lista de mostrados por escenario en `localStorage` |
| `components/ruleta/RuletaBanner.tsx` (crear) | banner + acordeón (situaciones ↔ tarjeta) |
| `components/ruleta/RuletaCard.tsx` (crear) | la tarjeta |
| `components/ruleta/frases.ts` (crear) | banco de frases de `atencion` |
| `components/CatalogView.tsx` (modificar) | montar el banner arriba de `DesempateBanner` |
| `app/globals.css` (modificar) | estilos de la tarjeta y las situaciones |
| `lib/enrich.ts` (modificar) | exportar `watchLinkFor()` sobre el `providersOf` que ya existe |
| `CLAUDE.md` (modificar) | documentar |

---

## Task 1: Mapa de plataformas

El punto más delicado del spec: durante el análisis, pasar `"movistar"` en vez de `"MovistarTV"` hizo desaparecer 15 títulos sin ningún error. MovistarTV es el proveedor más grande del pool.

**Files:**
- Create: `lib/roulette-providers.ts`

**Interfaces:**
- Consumes: `PlatformCode` de `lib/types.ts`; `ALL_CODES` de `lib/providers-ar.ts`.
- Produces: `roulettePlatformNames(codes: PlatformCode[]): string[]`

- [ ] **Step 1: Crear el archivo**

```ts
import type { PlatformCode } from "./types";
import { ALL_CODES } from "./providers-ar";

// Puente entre los códigos de plataforma de la app y los nombres que el
// pipeline offline guarda en `title_availability.providers` (tal como los
// devuelve TMDB/JustWatch).
//
// Es explícito y no derivado a propósito: los nombres no siguen ninguna regla.
// "Max" allá es "HBO Max", "Disney+" es "Disney Plus", y una misma plataforma
// puede aparecer con dos nombres (la propia y su channel dentro de Amazon o
// Apple), que cuentan como la misma.
//
// Revisado el 2026-08-09 contra la lista COMPLETA de
// `select distinct unnest(providers) from title_availability where region='AR'`:
// 40 nombres. Los que quedan deliberadamente afuera, porque no son plataformas
// de esta app (con su cantidad de títulos, para dimensionar lo que se pierde):
//
//   MGM+ Apple TV Channel 92 · MGM Plus Amazon Channel 92 · Mercado Play 92
//   Sony One Amazon Channel 60 · Plex 37 · Artiflix 27 · Pluto TV 18
//   JustWatch TV 17 · Bloodstream 12 · Cultpix 8 · Runtime 5 · FilmBox+ 4
//   Adrenalina Pura Amazon channel 3 · Adrenalina Pura Apple TV channel 3
//   Artify 3 · Filmzie 2 · Dekkoo 1 · DocAlliance Films 1 · FOUND TV 1
//   Shahid VIP 1
//
// Son servicios gratuitos con publicidad o channels de marcas que la app no
// ofrece; Pluto TV además está descartada explícitamente (ver providers-ar.ts).
// Si alguna se suma a `PLATFORMS`, hay que sumar acá su nombre EXACTO.
//
// Sobre los 765 títulos con texto, este mapa alcanza 696.
const NOMBRES: Record<PlatformCode, string[]> = {
  n:  ["Netflix"],
  d:  ["Disney Plus"],
  m:  ["HBO Max"],
  p:  ["Amazon Prime Video"],
  // OJO: acá "Apple TV" ES la suscripción. `title_availability` sólo guarda
  // flatrate/free/ads y excluye alquiler, así que no es el mismo caso que el
  // provider_id 2 de TMDB (la tienda), que se sacó de providers-ar.ts.
  at: ["Apple TV", "Apple TV Amazon Channel"],
  pp: ["Paramount Plus", "Paramount+ Amazon Channel"],
  mb: ["MUBI", "MUBI Amazon Channel"],
  un: ["Universal+ Amazon Channel"],
  mv: ["MovistarTV"],
  cv: ["Claro video"],
  // "VIX " con espacio al final NO es un typo: es cómo está el dato en la base.
  // Corregirlo acá haría desaparecer esos títulos en silencio.
  vx: ["VIX ", "ViX Premium Amazon Channel"],
  dg: ["DIRECTV GO"],
  cr: ["Crunchyroll", "Crunchyroll Amazon Channel"],
  ok: ["OnDemandKorea"],
  // Star+ se fusionó con Disney+ en 2024. Sigue en PlatformCode para no romper
  // el localStorage de quien la haya elegido antes; en el pool no existe.
  sp: [],
};

// Falla ruidosamente en desarrollo si alguien agrega un PlatformCode y se
// olvida de darle su nombre acá: sin esto, el título simplemente no aparece y
// no hay ningún error que lo delate.
//
// OJO con el alcance de este guard: detecta códigos SIN mapear, no mapeos
// INCORRECTOS. Un nombre mal escrito o incompleto pasa igual y hace desaparecer
// títulos en silencio. Contra eso no hay guard en tiempo de ejecución: hay que
// diffear a mano contra la base cada vez que se toca este archivo, con la
// consulta que está en el plan (Task 1, Step 3).
if (process.env.NODE_ENV !== "production") {
  const faltan = ALL_CODES.filter((c) => !NOMBRES[c]?.length);
  if (faltan.length) {
    throw new Error(
      `[ruleta] Plataformas sin nombre en title_availability: ${faltan.join(", ")}. ` +
      `Agregá su nombre EXACTO en lib/roulette-providers.ts (verificalo contra ` +
      `select distinct unnest(providers) from title_availability where region='AR').`,
    );
  }
}

export function roulettePlatformNames(codes: PlatformCode[]): string[] {
  return [...new Set(codes.flatMap((c) => NOMBRES[c] ?? []))];
}
```

- [ ] **Step 2: Verificar que compila**

Run: `npx tsc --noEmit`
Expected: sin salida.

- [ ] **Step 3: Diffear el mapa contra la lista completa de la base**

El guard del código detecta códigos sin mapear, **no nombres mal escritos**. Ese diff es a mano, y es obligatorio.

Corré en el SQL editor de Supabase:

```sql
select p.proveedor, count(*) as titulos
from title_availability a
cross join lateral unnest(a.providers) as p(proveedor)
where a.region = 'AR'
group by 1 order by 1;
```

Expected: **40 nombres**. Compará uno por uno contra `NOMBRES` y contra la lista de exclusiones deliberadas del comentario. Cada nombre de la base tiene que estar en una de las dos listas. Si aparece uno nuevo:

- Si corresponde a una plataforma de `PLATFORMS`, agregalo a `NOMBRES`.
- Si no, agregalo a la lista de exclusiones del comentario, con su cantidad.

**No sigas con un nombre sin clasificar.** Los dos errores que ya cazó este diff fueron `Crunchyroll` a secas (17 títulos, además del channel de Amazon) y `OnDemandKorea` (1), los dos invisibles si se mira sólo el pool con texto.

- [ ] **Step 4: Verificar el mapeo contra la base, sobre todo MovistarTV**

Es un criterio de aceptación explícito del spec. Con `npm run dev` levantado no alcanza (el módulo es server-side y todavía no hay ruta): comprobalo con este script, que reproduce el mapa y consulta la base con las credenciales de `.env.local`.

```bash
node --input-type=module -e "
import fs from 'node:fs';
const env = fs.readFileSync('.env.local','utf8');
const url = env.match(/^NEXT_PUBLIC_SUPABASE_URL=(.+)\$/m)[1].trim();
const key = env.match(/^NEXT_PUBLIC_SUPABASE_ANON_KEY=(.+)\$/m)[1].trim();
const nombres = ['Netflix','Disney Plus','HBO Max','Amazon Prime Video','Apple TV','Apple TV Amazon Channel','Paramount Plus','Paramount+ Amazon Channel','MUBI','MUBI Amazon Channel','Universal+ Amazon Channel','MovistarTV','Claro video','VIX ','ViX Premium Amazon Channel','DIRECTV GO','Crunchyroll Amazon Channel'];
for (const solo of ['MovistarTV','VIX ','Netflix']) {
  const r = await fetch(url + '/rest/v1/rpc/get_roulette_picks', {
    method:'POST', headers:{apikey:key,Authorization:'Bearer '+key,'Content-Type':'application/json'},
    body: JSON.stringify({p_providers:[solo],p_escenario:'solo',p_excluir:[],p_region:'AR',p_seed:'x',p_limit:20}),
  });
  const j = await r.json();
  console.log(JSON.stringify(solo), '->', Array.isArray(j) ? j.length + ' títulos' : j);
}
const r = await fetch(url + '/rest/v1/rpc/get_roulette_picks', {
  method:'POST', headers:{apikey:key,Authorization:'Bearer '+key,'Content-Type':'application/json'},
  body: JSON.stringify({p_providers:nombres,p_escenario:'solo',p_excluir:[],p_region:'AR',p_seed:'x',p_limit:20}),
});
console.log('mapa completo ->', (await r.json()).length, 'títulos');
"
```

Expected: `"MovistarTV" -> 20 títulos`, `"VIX " -> N títulos` con N ≥ 1, `"Netflix" -> 20 títulos`, y `mapa completo -> 20 títulos`. **Si MovistarTV o `"VIX "` devuelven 0, el mapa está mal**: revisá el nombre carácter por carácter antes de seguir.

- [ ] **Step 5: Commit**

```bash
git add lib/roulette-providers.ts
git commit -m "feat(ruleta): mapa de plataformas hacia title_availability"
```

---

## Task 2: Banco de frases de atención

**Files:**
- Create: `components/ruleta/frases.ts`

**Interfaces:**
- Produces: `fraseAtencion(atencion: string | null, tmdbId: number): string | null`

- [ ] **Step 1: Crear el archivo**

```ts
// `atencion` es dato (alta/media/fondo) y sirve para filtrar del lado de la
// base. NUNCA se muestra crudo: acá se traduce a una frase escrita a mano.
// El banco vive en el código y no en la base para que la voz sea una sola.
const BANCO: Record<string, string[]> = {
  alta:  ["Pide cabeza", "Si te das vuelta un segundo, ya no entendés nada"],
  media: ["Se sigue sola", "Ni liviana ni pesada"],
  fondo: ["Se mira fácil", "Aguanta que cocines"],
};

// La variante se elige por tmdb_id y no al azar: así el mismo título muestra
// siempre la misma frase, y no cambia sola al re-renderizar la tarjeta.
export function fraseAtencion(atencion: string | null, tmdbId: number): string | null {
  const frases = atencion ? BANCO[atencion] : undefined;
  if (!frases?.length) return null;
  return frases[tmdbId % frases.length];
}
```

- [ ] **Step 2: Verificar que compila**

Run: `npx tsc --noEmit`
Expected: sin salida.

- [ ] **Step 3: Commit**

```bash
git add components/ruleta/frases.ts
git commit -m "feat(ruleta): banco de frases de atención"
```

---

## Task 3: `lib/roulette.ts` y `/api/ruleta`

**Files:**
- Modify: `lib/enrich.ts` (exportar `watchLinkFor`)
- Create: `lib/roulette.ts`
- Create: `app/api/ruleta/route.ts`

**Interfaces:**
- Consumes: `roulettePlatformNames` (Task 1); `cardsByIds` de `lib/enrich.ts`; `supabaseServer` de `lib/supabase.ts`; `dailySeed` de `lib/cache.ts`.
- Produces:
  - `watchLinkFor(type: MediaType, id: number): Promise<string | null>` en `lib/enrich.ts`
  - `type Escenario = "solo" | "pareja" | "chicos" | "fondo"`
  - `type RoulettePick = { id: number; type: MediaType; title: string; year: number | null; runtime: string | null; poster: string | null; genres: string[]; platforms: PlatformCode[]; razon: string; advertencia: string | null; atencion: string | null; watchLink: string | null }`
  - `getRoulettePicks(opts: { escenario: Escenario; providers: PlatformCode[]; excluir: number[] }): Promise<RoulettePick[]>`

- [ ] **Step 1: Exportar `watchLinkFor` desde `lib/enrich.ts`**

Justo debajo de la función `providersOf` (línea 45-57), agregar:

```ts
// El link del agregador para un título. Reusa el MISMO `cached` de providersOf
// (clave `pv:${type}:${id}`), así que en un listado ya enriquecido con
// cardsByIds no cuesta ningún request extra a TMDB.
export async function watchLinkFor(type: MediaType, id: number): Promise<string | null> {
  return (await providersOf(type, id)).watchLink;
}
```

- [ ] **Step 2: Crear `lib/roulette.ts`**

```ts
import "server-only";
import { cardsByIds, watchLinkFor } from "./enrich";
import { roulettePlatformNames } from "./roulette-providers";
import { supabaseServer } from "./supabase";
import { dailySeed } from "./cache";
import type { MediaType, PlatformCode } from "./types";

export type Escenario = "solo" | "pareja" | "chicos" | "fondo";
export const ESCENARIOS: Escenario[] = ["solo", "pareja", "chicos", "fondo"];
export const esEscenario = (s: string): s is Escenario =>
  (ESCENARIOS as string[]).includes(s);

// Cuántos candidatos pide cada tanda. El cliente los consume de a uno con
// "Otra": una query por tanda, no una por toque.
const TANDA = 20;

export interface RoulettePick {
  id: number;
  type: MediaType;
  title: string;
  year: number | null;
  runtime: string | null;
  poster: string | null;
  genres: string[];
  platforms: PlatformCode[];
  razon: string;
  advertencia: string | null;
  atencion: string | null;
  watchLink: string | null;
}

// Lo que devuelve la RPC. `title`, `genres` y `vote_average` de acá NO se usan
// para mostrar: son snapshot de auditoría del pipeline. Lo visible sale de TMDB.
interface FilaRpc {
  tmdb_id: number;
  media_type: MediaType;
  razon: string;
  advertencia: string | null;
  atencion: string | null;
}

export async function getRoulettePicks(opts: {
  escenario: Escenario;
  providers: PlatformCode[];
  excluir: number[];
}): Promise<RoulettePick[]> {
  const db = supabaseServer();
  if (!db) return [];
  const nombres = roulettePlatformNames(opts.providers);
  if (!nombres.length) return [];

  const { data, error } = await db.rpc("get_roulette_picks", {
    p_providers: nombres,
    p_escenario: opts.escenario,
    p_excluir: opts.excluir,
    p_region: "AR",
    // La MISMA semilla del día que usa el resto de la app: compartida a
    // propósito, es lo que permite que el cache sirva a varios usuarios.
    p_seed: String(dailySeed()),
    p_limit: TANDA,
  });
  if (error) throw new Error(`get_roulette_picks falló: ${error.message}`);

  const filas = (data ?? []) as FilaRpc[];
  if (!filas.length) return [];

  // Enriquecido con la misma función cacheada en Redis que usan los rieles del
  // Home: respeta idioma y región, y no duplica cache.
  const cards = await cardsByIds(
    filas.map((f) => ({ tipo: f.media_type, id: f.tmdb_id })),
  );
  const porId = new Map(cards.map((c) => [c.id, c]));

  const picks = await Promise.all(filas.map(async (f) => {
    const card = porId.get(f.tmdb_id);
    // Un título que TMDB no pudo enriquecer se descarta: sin póster ni título
    // la tarjeta no se sostiene, y hay 19 más en la tanda.
    if (!card) return null;
    return {
      id: card.id,
      type: card.type,
      title: card.title,
      year: card.year,
      runtime: card.runtime,
      poster: card.poster,
      genres: card.genres,
      platforms: card.platforms,
      razon: f.razon,
      advertencia: f.advertencia,
      atencion: f.atencion,
      watchLink: await watchLinkFor(card.type, card.id),
    } satisfies RoulettePick;
  }));

  return picks.filter((p): p is RoulettePick => p !== null);
}
```

- [ ] **Step 3: Crear `app/api/ruleta/route.ts`**

```ts
import { NextRequest, NextResponse } from "next/server";
import { getRoulettePicks, esEscenario } from "@/lib/roulette";
import type { PlatformCode } from "@/lib/types";

export const dynamic = "force-dynamic";
export const maxDuration = 60;

export async function GET(req: NextRequest) {
  const q = req.nextUrl.searchParams;
  const escenarioRaw = q.get("escenario") ?? "solo";
  const escenario = esEscenario(escenarioRaw) ? escenarioRaw : "solo";
  const providers = (q.get("providers") || "")
    .split(",").map((s) => s.trim()).filter(Boolean) as PlatformCode[];
  const excluir = (q.get("excluir") || "")
    .split(",").map((s) => Number(s.trim())).filter(Number.isFinite);

  try {
    return NextResponse.json({ picks: await getRoulettePicks({ escenario, providers, excluir }) });
  } catch (e) {
    return NextResponse.json({ error: String(e), picks: [] }, { status: 500 });
  }
}
```

- [ ] **Step 4: Verificar que compila**

Run: `npx tsc --noEmit`
Expected: sin salida.

- [ ] **Step 5: Probar el endpoint en los cuatro escenarios**

Con `npm run dev`:

```bash
for e in solo pareja chicos fondo; do echo -n "$e: "; curl -s "http://localhost:3000/api/ruleta?escenario=$e&providers=n,d,m,mv" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);const p=j.picks||[];console.log(p.length+' picks, con advertencia: '+p.filter(x=>x.advertencia).length+', con link: '+p.filter(x=>x.watchLink).length+', 1ro: '+(p[0]?p[0].title:'-'))})"; done
```

Expected: los cuatro escenarios devuelven picks (cerca de 20), una parte con advertencia y una parte sin (es NULL en ~27% por diseño), y la mayoría con `watchLink`.

- [ ] **Step 6: Probar `excluir` y `providers` vacío**

```bash
curl -s "http://localhost:3000/api/ruleta?escenario=solo&providers=n,d,m,mv" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>console.log('primer id:',JSON.parse(s).picks[0].id))"
```

Tomá ese id y volvé a pedir excluyéndolo:

```bash
curl -s "http://localhost:3000/api/ruleta?escenario=solo&providers=n,d,m,mv&excluir=<ID>" | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const p=JSON.parse(s).picks;console.log('primer id ahora:',p[0].id,'| el excluido aparece:',p.some(x=>x.id===<ID>))})"
```

Expected: el primer id cambia y `el excluido aparece: false`.

```bash
curl -s "http://localhost:3000/api/ruleta?escenario=solo&providers=" | head -c 80
```

Expected: `{"picks":[]}` — sin plataformas no hay nada que recomendar, y no rompe.

- [ ] **Step 7: Commit**

```bash
git add lib/enrich.ts lib/roulette.ts app/api/ruleta/route.ts
git commit -m "feat(ruleta): RPC, enriquecido y endpoint /api/ruleta"
```

---

## Task 4: Lista de mostrados en `localStorage`

Sin esto, con la semilla diaria compartida, quien vuelve a la ruleta el mismo día recibe exactamente la misma primera recomendación. Es **estado de paginación**, no historial: por eso vive aparte de `user_items` y no lo contradice.

**Files:**
- Create: `hooks/useRouletteSeen.ts`

**Interfaces:**
- Consumes: `Escenario` de `lib/roulette.ts` (sólo el tipo).
- Produces: `useRouletteSeen(): { seen: (e: Escenario) => number[]; add: (e: Escenario, ids: number[]) => void; reset: (e: Escenario) => void }`

- [ ] **Step 1: Crear el hook**

```ts
"use client";
import { useCallback, useEffect, useRef } from "react";
import type { Escenario } from "@/lib/roulette";

// Los tmdb_id que la ruleta YA mostró, por escenario.
//
// Es estado de paginación, no historial: la semilla del día es compartida y
// estable, así que sin esto volver a la ruleta a la tarde devuelve la misma
// película de la mañana. Se pasa en p_excluir junto con los "ya la vi" del
// usuario, y aplica igual a quien está logueado.
//
// Por escenario y no en una bolsa única: agotar `fondo` (el más angosto, 123
// títulos alcanzables) borraría el progreso de los otros tres.
const KEY = "yump:ruleta-mostrados";
// Tope por escenario. El pool alcanzable más chico son 123 títulos, así que
// 300 no recorta nada real; está para que la clave no crezca sin límite.
const TOPE = 300;

type Store = Partial<Record<Escenario, number[]>>;

function read(): Store {
  try {
    const raw = localStorage.getItem(KEY);
    return raw ? (JSON.parse(raw) as Store) : {};
  } catch {
    return {};
  }
}

export function useRouletteSeen() {
  // Copia en memoria para no leer localStorage en cada render. Se hidrata tras
  // montar: en el server no existe.
  const store = useRef<Store>({});
  useEffect(() => { store.current = read(); }, []);

  const persist = useCallback(() => {
    try { localStorage.setItem(KEY, JSON.stringify(store.current)); } catch { /* modo privado: no persiste, no rompe */ }
  }, []);

  const seen = useCallback((e: Escenario) => store.current[e] ?? [], []);

  const add = useCallback((e: Escenario, ids: number[]) => {
    const previos = store.current[e] ?? [];
    const union = [...new Set([...previos, ...ids])];
    store.current[e] = union.slice(-TOPE);
    persist();
  }, [persist]);

  const reset = useCallback((e: Escenario) => {
    store.current[e] = [];
    persist();
  }, [persist]);

  return { seen, add, reset };
}
```

- [ ] **Step 2: Verificar que compila**

Run: `npx tsc --noEmit`
Expected: sin salida.

- [ ] **Step 3: Commit**

```bash
git add hooks/useRouletteSeen.ts
git commit -m "feat(ruleta): lista de mostrados por escenario en localStorage"
```

---

## Task 5: La tarjeta

**Files:**
- Create: `components/ruleta/RuletaCard.tsx`
- Modify: `app/globals.css`

**Interfaces:**
- Consumes: `RoulettePick` de `lib/roulette.ts`; `fraseAtencion` de `./frases`; `useAuth`, `PlatformLogo`, `setItem` de `lib/userdata`.
- Produces: `<RuletaCard pick onOtra onCerrar />`

- [ ] **Step 1: Crear el componente**

El botón "Ya la vi" replica **exactamente** lo que hace hoy el de la ficha (`components/ListActions.tsx:30`): siempre visible, y sin sesión `router.push("/cuenta")`.

```tsx
"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import Link from "next/link";
import { useAuth } from "../AuthContext";
import PlatformLogo from "../PlatformLogo";
import { setItem } from "@/lib/userdata";
import { fraseAtencion } from "./frases";
import type { RoulettePick } from "@/lib/roulette";

export default function RuletaCard({
  pick, onOtra, onCerrar,
}: { pick: RoulettePick; onOtra: () => void; onCerrar: () => void }) {
  const { user } = useAuth();
  const router = useRouter();
  const [visto, setVisto] = useState(false);
  const [busy, setBusy] = useState(false);
  const frase = fraseAtencion(pick.atencion, pick.id);
  const plataforma = pick.platforms[0];

  // Copia exacta de toggleWatched de components/ListActions.tsx:30 — mismo dato,
  // misma acción. Es un TOGGLE de ida y vuelta: un mistap se deshace tocando de
  // nuevo, sin tener que ir a buscar la ficha. Y por eso la tarjeta NO avanza
  // sola al marcar: si avanzara, deshacer sería imposible acá.
  async function toggleVisto() {
    if (!user) { router.push("/cuenta"); return; }
    if (busy) return;
    setBusy(true);
    const next = !visto;
    setVisto(next); // optimista
    const { error } = await setItem(user.id, "watched", { tmdb_id: pick.id, tipo: pick.type }, next);
    if (error) setVisto(!next); // rollback
    setBusy(false);
  }

  const meta = [
    pick.year, pick.runtime, pick.genres.length ? pick.genres.join(" / ") : null,
  ].filter(Boolean).join(" · ");

  return (
    <div className="rlt-card">
      <button className="rlt-close" onClick={onCerrar} aria-label="Cerrar">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round"><path d="M18 6L6 18M6 6l12 12" /></svg>
      </button>

      <span className="chip-group-label">Te recomendamos</span>
      <h3 className="rlt-title">
        <Link href={`/titulo/${pick.type}/${pick.id}`}>{pick.title}</Link>
      </h3>
      {meta && <p className="rlt-meta">{meta}</p>}

      <div className="rlt-tags">
        {plataforma && <span className="rlt-tag rlt-tag-plat"><PlatformLogo code={plataforma} /></span>}
        {frase && <span className="rlt-tag">{frase}</span>}
      </div>

      <span className="chip-group-label">Por qué esta</span>
      <p className="rlt-razon">{pick.razon}</p>

      {/* El bloque PERO sólo existe si hay advertencia: viene NULL en ~27% de
          los casos por diseño, y preferimos que falte a inventarla. */}
      {pick.advertencia && (
        <div className="rlt-pero">
          <span className="rlt-pero-lab">⚠ Pero</span>
          <p>{pick.advertencia}</p>
        </div>
      )}

      <div className="rlt-acciones">
        <a
          className="rlt-btn rlt-btn-primary"
          href={pick.watchLink ?? `/titulo/${pick.type}/${pick.id}`}
          target={pick.watchLink ? "_blank" : undefined}
          rel={pick.watchLink ? "noreferrer" : undefined}
        >
          ▶ Verla
        </a>
        <button className="rlt-btn" onClick={onOtra}>Otra</button>
        <button className={`rlt-btn ${visto ? "on" : ""}`} onClick={toggleVisto} disabled={busy}>
          {visto ? "✓ Ya la viste" : "Ya la vi"}
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Agregar el CSS**

En `app/globals.css`, después del bloque de `.dsmp-*`:

```css
/* --- Ruleta "no sé qué ver" --- */
/* La tarjeta se despliega dentro del banner (acordeón), como el de Desempatá:
   por eso no lleva borde arriba y continúa el radio del banner. */
.rlt-panel{border:1px solid var(--accent);border-top:0;border-bottom-left-radius:var(--radius);border-bottom-right-radius:var(--radius);background:var(--surface);padding:20px}
.rlt-sit{display:grid;grid-template-columns:repeat(2,1fr);gap:10px}
.rlt-sit button{display:flex;flex-direction:column;align-items:flex-start;gap:3px;padding:14px;border-radius:var(--radius-sm);border:1px solid var(--line-2);background:var(--bg);color:var(--text);cursor:pointer;text-align:left;font-family:var(--body)}
.rlt-sit button:hover{border-color:var(--accent)}
.rlt-sit-ico{font-size:20px;line-height:1}
.rlt-sit-tit{font-weight:600;font-size:14px;letter-spacing:-.01em}
.rlt-sit-sub{font-size:12px;color:var(--dim)}
.rlt-card{position:relative}
.rlt-close{position:absolute;top:0;right:0;width:32px;height:32px;border-radius:50%;border:1px solid var(--line-2);background:var(--bg);color:var(--dim);cursor:pointer;display:flex;align-items:center;justify-content:center}
.rlt-close svg{width:15px;height:15px}
.rlt-close:hover{border-color:var(--accent);color:var(--accent)}
.rlt-title{font-family:var(--display);font-size:30px;font-weight:800;letter-spacing:-.02em;margin:2px 0 4px;line-height:1.1}
.rlt-meta{color:var(--dim);font-size:13px;margin:0 0 14px}
.rlt-tags{display:flex;flex-wrap:wrap;gap:8px;margin-bottom:18px}
.rlt-tag{display:inline-flex;align-items:center;height:30px;padding:0 12px;border-radius:999px;border:1px solid var(--line-2);background:var(--bg);font-size:12.5px;color:var(--dim)}
.rlt-tag-plat{border-color:var(--accent)}
.rlt-razon{font-size:15px;line-height:1.55;margin:2px 0 18px}
.rlt-pero{border:1px solid var(--line-2);border-left:3px solid var(--accent);border-radius:var(--radius-sm);padding:12px 14px;margin-bottom:18px;background:var(--surface-2)}
.rlt-pero-lab{display:block;font-size:11px;font-weight:700;letter-spacing:.06em;text-transform:uppercase;color:var(--accent);margin-bottom:5px}
.rlt-pero p{margin:0;font-size:13.5px;line-height:1.5;color:var(--text)}
.rlt-acciones{display:flex;gap:10px;flex-wrap:wrap}
.rlt-btn{display:inline-flex;align-items:center;justify-content:center;gap:7px;height:46px;padding:0 20px;border-radius:999px;border:1px solid var(--line-2);background:var(--bg);color:var(--text);font-family:var(--body);font-size:14px;font-weight:600;cursor:pointer}
.rlt-btn:hover{border-color:var(--accent)}
.rlt-btn:disabled{opacity:.55;cursor:default}
/* "Ya la vi" marcado. Es un toggle: se vuelve a tocar para deshacer. */
.rlt-btn.on{border-color:var(--good);color:var(--good)}
.rlt-btn-primary{flex:1;min-width:180px;background:var(--accent);border-color:var(--accent);color:#fff}
.rlt-btn-primary:hover{filter:brightness(1.06);border-color:var(--accent)}
@media (max-width:620px){
  .rlt-sit{grid-template-columns:1fr}
  .rlt-title{font-size:25px}
  .rlt-btn-primary{min-width:100%}
}
```

- [ ] **Step 3: Verificar que compila**

Run: `npx tsc --noEmit`
Expected: sin salida.

- [ ] **Step 4: Commit**

```bash
git add components/ruleta/RuletaCard.tsx app/globals.css
git commit -m "feat(ruleta): tarjeta de recomendación"
```

---

## Task 6: El banner y el montaje en el Home

**Files:**
- Create: `components/ruleta/RuletaBanner.tsx`
- Modify: `components/CatalogView.tsx`

**Interfaces:**
- Consumes: `RuletaCard` (Task 5); `useRouletteSeen` (Task 4); `usePlatforms`; `useAuth`; `itemRefs` de `lib/userdata`; `ESCENARIOS`/`Escenario`/`RoulettePick` de `lib/roulette.ts`.
- Produces: `<RuletaBanner />`

- [ ] **Step 1: Crear el banner**

Reusa las clases `.dsmp-banner*` que ya existen (`app/globals.css:432`): son exactamente el patrón emoji + título + botón que pidió el dueño.

```tsx
"use client";
import { useCallback, useEffect, useRef, useState } from "react";
import { usePlatforms } from "../PlatformsContext";
import { useAuth } from "../AuthContext";
import { itemRefs } from "@/lib/userdata";
import { useRouletteSeen } from "@/hooks/useRouletteSeen";
import RuletaCard from "./RuletaCard";
import type { Escenario, RoulettePick } from "@/lib/roulette";

const SITUACIONES: { id: Escenario; ico: string; tit: string; sub: string }[] = [
  { id: "solo",   ico: "🎯", tit: "Solo/a",       sub: "Quiero algo bueno" },
  { id: "pareja", ico: "❤️", tit: "En pareja",    sub: "Algo para los dos" },
  { id: "chicos", ico: "🧸", tit: "Con chicos",   sub: "Que la vean todos" },
  { id: "fondo",  ico: "🍳", tit: "De fondo",     sub: "Mientras hago otra cosa" },
];

export default function RuletaBanner() {
  const { platforms } = usePlatforms();
  const { user } = useAuth();
  const { seen, add, reset } = useRouletteSeen();
  // Los "ya la vi" del usuario, que también van en p_excluir: la ruleta arranca
  // sabiendo lo que ya vio desde el primer uso. Se cargan una vez al abrir el
  // panel, no en cada tanda. Sin sesión queda vacío y no pasa nada.
  const vistos = useRef<number[]>([]);
  const [open, setOpen] = useState(false);
  const [escenario, setEscenario] = useState<Escenario | null>(null);
  const [cola, setCola] = useState<RoulettePick[]>([]);
  const [actual, setActual] = useState<RoulettePick | null>(null);
  const [cargando, setCargando] = useState(false);
  const [error, setError] = useState(false);

  // Una query por tanda. `reintento` evita el bucle infinito: si tras limpiar
  // los mostrados sigue sin venir nada, es que no hay pool y se muestra el
  // estado vacío.
  useEffect(() => {
    if (!open || !user) { if (!user) vistos.current = []; return; }
    let alive = true;
    void itemRefs("watched").then((refs) => {
      if (alive) vistos.current = refs.map((r) => r.tmdb_id);
    });
    return () => { alive = false; };
  }, [open, user]);

  const pedirTanda = useCallback(async (e: Escenario, reintento = false): Promise<RoulettePick[]> => {
    // p_excluir = lo que ya vio + lo que la ruleta ya le mostró en ESTE
    // escenario. Lo primero es historial real (user_items); lo segundo, estado
    // de paginación. El reset por agotamiento sólo limpia lo segundo.
    const mostrados = seen(e);
    const excluir = [...new Set([...vistos.current, ...mostrados])];
    const url = `/api/ruleta?escenario=${e}&providers=${platforms.join(",")}`
      + (excluir.length ? `&excluir=${excluir.join(",")}` : "");
    const r = await fetch(url);
    if (!r.ok) throw new Error(String(r.status));
    const picks: RoulettePick[] = (await r.json()).picks ?? [];
    // Pool agotado: se limpia el escenario y se vuelve a empezar. Sin esto la
    // ruleta llega a un callejón sin salida, porque p_excluir sólo crece.
    // Se reintenta sólo si había mostrados que limpiar: si lo único que excluía
    // eran los "ya la vi", limpiar no cambia nada y sería un bucle.
    if (!picks.length && mostrados.length && !reintento) {
      reset(e);
      return pedirTanda(e, true);
    }
    return picks;
  }, [platforms, seen, reset]);

  const elegir = useCallback(async (e: Escenario) => {
    setEscenario(e); setCargando(true); setError(false);
    try {
      const picks = await pedirTanda(e);
      const [primero, ...resto] = picks;
      if (primero) add(e, [primero.id]);
      setActual(primero ?? null);
      setCola(resto);
    } catch {
      setError(true); setActual(null); setCola([]);
    } finally {
      setCargando(false);
    }
  }, [pedirTanda, add]);

  const otra = useCallback(async () => {
    if (!escenario) return;
    // Consume de la tanda que ya está en el cliente; sólo pide otra al agotarse.
    if (cola.length) {
      const [siguiente, ...resto] = cola;
      add(escenario, [siguiente.id]);
      setActual(siguiente); setCola(resto);
      return;
    }
    await elegir(escenario);
  }, [escenario, cola, add, elegir]);

  const volver = () => { setEscenario(null); setActual(null); setCola([]); setError(false); };
  const cerrar = () => { setOpen(false); volver(); };

  return (
    <div className="dsmp">
      <button className={`dsmp-banner ${open ? "open" : ""}`} onClick={() => (open ? cerrar() : setOpen(true))}>
        <span className="dsmp-banner-ico" aria-hidden>🎲</span>
        <span className="dsmp-banner-txt">
          <span className="dsmp-banner-title">¿No sabés qué ver?</span>
          <span className="dsmp-banner-sub">Una sola recomendación, sin scrollear.</span>
        </span>
        <span className="dsmp-banner-cta">
          {open ? "Cerrar" : "Probá"}
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M6 9l6 6 6-6" /></svg>
        </span>
      </button>

      {open && (
        <div className="rlt-panel">
          {!escenario ? (
            <>
              <span className="chip-group-label">¿Cómo la vas a ver?</span>
              <div className="rlt-sit">
                {SITUACIONES.map((s) => (
                  <button key={s.id} type="button" onClick={() => void elegir(s.id)}>
                    <span className="rlt-sit-ico" aria-hidden>{s.ico}</span>
                    <span className="rlt-sit-tit">{s.tit}</span>
                    <span className="rlt-sit-sub">{s.sub}</span>
                  </button>
                ))}
              </div>
            </>
          ) : cargando ? (
            <span className="loading">Buscando algo bueno…</span>
          ) : error ? (
            <p className="empty-note">
              No pudimos traer una recomendación. <button className="rlt-btn" onClick={() => void elegir(escenario)}>Reintentar</button>
            </p>
          ) : actual ? (
            <RuletaCard pick={actual} onOtra={() => void otra()} onCerrar={volver} />
          ) : (
            <p className="empty-note">
              No hay nada para esta situación en tus plataformas. <button className="rlt-btn" onClick={volver}>Probar otra</button>
            </p>
          )}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: Montarlo en el Home**

En `components/CatalogView.tsx`, importar y renderizar **arriba** de `<DesempateBanner />` (línea 69):

```tsx
import RuletaBanner from "./ruleta/RuletaBanner";
```

```tsx
        <RuletaBanner />
        <DesempateBanner />
```

**No toques nada más de `CatalogView`**: ni los rieles, ni el hero, ni el payload de `/api/home`.

- [ ] **Step 3: Verificar**

Run: `npx tsc --noEmit`
Expected: sin salida.

Run: `npx next build`
Expected: compila. Es el paso que caza el arrastre de módulos server-only al bundle del navegador, que `tsc` no ve. **`RuletaBanner` importa sólo el TIPO desde `lib/roulette.ts`** (`import type`), que se borra en compilación; si en algún momento se importa un valor de ahí, el build va a fallar y está bien que falle.

Con `npm run dev`, en `http://localhost:3000`:
- El banner de la ruleta aparece **arriba** del de "¿No te decidís?".
- Al abrirlo se ven las cuatro situaciones, no una lista de títulos.
- Al elegir una aparece **una** tarjeta.
- "Otra" cambia el título **sin disparar un request** (miralo en la pestaña Red de DevTools: no debe haber una llamada a `/api/ruleta` hasta la vigésima).
- Recargar la página y volver a elegir el mismo escenario **no** devuelve el mismo primer título (mirá `localStorage`, key `yump:ruleta-mostrados`).
- Alguna tarjeta con `advertencia` NULL no muestra el bloque "PERO". Para encontrar una: en la consola, `fetch('/api/ruleta?escenario=solo&providers=n,d,m,mv').then(r=>r.json()).then(j=>console.log(j.picks.filter(p=>!p.advertencia).map(p=>p.title)))`.
- **Los 16 chips del Home y todos los rieles siguen igual.**

- [ ] **Step 4: Commit**

```bash
git add components/ruleta/RuletaBanner.tsx components/CatalogView.tsx
git commit -m "feat(ruleta): banner en el Home con las cuatro situaciones"
```

---

## Task 7: Verificación de aceptación y documentación

**Files:**
- Modify: `CLAUDE.md`

- [ ] **Step 1: Recorrer los criterios de aceptación del spec**

Uno por uno, anotando la evidencia:

1. Los 16 chips del Home se comportan igual → abrir el Home, tocar 3 chips distintos del recomendador y confirmar que devuelven lo de siempre.
2. Los cuatro escenarios devuelven resultados con texto → el curl del Task 3 Step 5.
3. "Otra" no dispara query hasta agotar la tanda → pestaña Red, 20 toques.
4. Un título marcado como visto no vuelve a salir → marcar "Ya la vi" logueado, recargar la página, pedir el mismo escenario y confirmar que no aparece en ninguna de las 20 posiciones de la tanda.
5. "Ya la vi" desde la tarjeta se refleja en la ficha, y viceversa → marcar en la tarjeta, abrir `/titulo/movie/<id>` y ver el botón activo; desmarcar desde la ficha y confirmar que el título vuelve a estar disponible para la ruleta.
5b. **"Ya la vi" se puede deshacer** → en la tarjeta, tocar el botón dos veces: queda como al principio, y en `user_items` no queda la fila. La tarjeta no debe cambiar de título al marcar.
6. `advertencia` NULL → sin bloque "PERO".
7. Ningún nombre de plataforma se pierde; MovistarTV matchea → el script del Task 1 Step 3.
8. Nunca se muestra el valor crudo de `atencion` → `grep -rn "alta\|fondo" components/ruleta/` no debe encontrar ninguna salida a pantalla.

- [ ] **Step 2: Documentar en `CLAUDE.md`**

En la tabla de rutas API:

```markdown
| `GET /api/ruleta` | una tanda de 20 candidatos del pool curado (`roulette_titles`), vía `lib/roulette.ts` |
```

En "Decisiones de arquitectura que importan":

```markdown
- **La ruleta sirve UNA recomendación por vez, nunca una lista.** Es el punto:
  una lista reconstruye la parálisis de elección que la feature resuelve. El pool
  es `roulette_titles` (curado offline, 765 con texto de los 1811) y lo sirve la
  función `get_roulette_picks`; la app **no la modifica**. Pide 20 candidatos por
  tanda y "Otra" los consume en el cliente: una query por tanda, no por toque.
  Los nombres de plataforma que guarda `title_availability` **no** son los de
  `lib/providers-ar.ts` (allá "Max" es "HBO Max", y `"VIX "` viene con un espacio
  al final que no hay que corregir): el puente explícito está en
  `lib/roulette-providers.ts` y falla ruidosamente en desarrollo si falta uno.
  `atencion` (`alta`/`media`/`fondo`) es dato para filtrar y **nunca se muestra
  crudo** — se traduce con el banco de frases de `components/ruleta/frases.ts`.
  `advertencia` es NULL en ~27% de los casos por diseño: ahí el bloque "PERO"
  simplemente no se renderiza, sin relleno. Los tmdb_id ya mostrados viven en
  `localStorage` (`yump:ruleta-mostrados`, por escenario) y se pasan en
  `p_excluir` junto con los "ya la vi": es estado de paginación, no historial —
  sin eso, la semilla diaria compartida devuelve la misma película todo el día.
```

En "Estructura", agregar `lib/roulette.ts`, `lib/roulette-providers.ts` y `components/ruleta/`.

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md
git commit -m "docs: documentar la ruleta en CLAUDE.md"
```

---

## Autorrevisión del plan

**Cobertura del spec:**

| Requisito | Tarea |
|---|---|
| Alcance: no tocar chips, `chip_titles`, `chip_blocklist`, `scripts/`, `prompts/` | Global Constraints; ninguna tarea los toca |
| `title_availability` sólo lectura | ninguna tarea escribe en la base |
| Cuatro situaciones, una pregunta | 6 (`SITUACIONES`) |
| Una tarjeta, tres acciones | 5 (`RuletaCard`) |
| Traer 20, consumir de a uno | 3 (`TANDA`) + 6 (`otra()`) |
| Mapeo de nombres, guard ruidoso | 1 |
| MovistarTV matchea (verificado) | 1 Step 3 |
| "Ya la vi" en el mismo lugar que la ficha | 5 (`setItem(..., "watched", ...)`) |
| Deslogueado: mismo comportamiento que la ficha | 5 (`router.push("/cuenta")`) |
| "Ya la vi" se puede deshacer | 5 (es toggle, y la tarjeta no avanza sola) |
| `p_excluir` con los **vistos** (`user_items`) | 6 (`vistos.current` en `pedirTanda`) |
| `p_excluir` con los **mostrados** (paginación) | 4 + 6 (`seen()` en `pedirTanda`) |
| Semilla compartida del día | 3 (`dailySeed()`) |
| Frases, no rótulos | 2 |
| `advertencia` NULL sin bloque | 5 |
| Enriquecido con TMDB cacheado | 3 (`cardsByIds`) |
| Deep link a la plataforma | 3 (`watchLinkFor`) |
| Mostrados en `localStorage`, por escenario | 4 |
| Reset al agotar el pool | 6 (`pedirTanda` con `reintento`) |

Sin huecos.

**Consistencia de tipos:** `Escenario` se define una vez en `lib/roulette.ts` y lo importan el hook y los dos componentes **como tipo** (`import type`), así que el cliente nunca arrastra el módulo server-only. `RoulettePick` es el único shape que cruza la ruta API. La RPC devuelve `media_type`, que se traduce a `type` una sola vez, dentro de `getRoulettePicks`.

**Riesgo conocido:** el spec pide que el mapeo falle ruidosamente en desarrollo. El guard del Task 1 corre al importar el módulo, así que un código sin mapear rompe el arranque de la ruta en `next dev` — que es lo buscado, pero conviene saberlo antes de agregar una plataforma nueva a `PLATFORMS`.
