// Reemplazo de `server-only` para los scripts de medición. El paquete real
// lanza cuando se lo importa fuera de un React Server Component, que es
// exactamente lo que hace un script de Node. Ver scripts/cargar-lib.mjs.
export {};
